version: '3'

services:
  web:
    restart: always
    build: .
    command: python manage.py runserver 0.0.0.0:80
    expose: 
      - 80
    env_file:
      - ./test.env
    volumes:
      - .:/code

networks:
  default:
    external:
      name: nginx-proxy
